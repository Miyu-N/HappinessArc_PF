<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="DMM WEBCAMPにて作成したポートフォリオです。">
    <title>HappinessArc</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag 'favicon.ico', :rel => 'icon' %>
    
  </head>

  <body>
    <header class="sticky-top border-bottom bg-white">
      <%= render 'layouts/header' %>
    </header>
    
      <main class="bg-light">
        <%= render 'layouts/flash_messages' %>
        <%= yield %>
        <% unless admin_signed_in? %>
          <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.4/css/all.css">
          <div id="page-contact"><a href="/contacts/new"></a></div>
        <% end %>
      </main>
    
    <%= render 'layouts/footer' %>
  </body>
</html>
